name: Looty
version: "${plugin_version}"
author: Offz
main: com.mineinabyss.looty.LootyPlugin
api-version: "1.20"

prefix: Looty
description: Custom items for Geary ECS

dependencies:
  server:
    Geary:
      required: true
      load: BEFORE
      join-classpath: true
